pub mod back_of_house {
    use std::error::Error;
    use std::fs::File;
    use std::io::prelude::*;

    // pyo3
    use pyo3::prelude::*;
    use pyo3::wrap_pyfunction;

    fn quadratic() {
        println!();
    }

    //    fn run() -> Result<i32, Box<Error>> {}
}
